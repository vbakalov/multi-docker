sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t vbakalov/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true vbakalov/react-test npm test -- --coverage

after_success:
  - docker build -t vbakalov/multi-client ./client
  - docker build -t vbakalov/multi-nginx ./nginx
  - docker build -t vbakalov/multi-server ./server
  - docker build -t vbakalov/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push vbakalov/multi-client
  - docker push vbakalov/multi-nginx
  - docker push vbakalov/multi-server
  - docker push vbakalov/multi-worker
